Trastea statically links the Rust crates listed below, so the binary carries their
code and their licences travel with it. Trastea's own terms are in LICENSE and the
embedded fonts' are in LICENSE-fonts.txt; neither covers anything on this page.

This file is generated by `just notices` from about.toml — edit those, not this.

Licences in use:
{{#each overview}}
  - {{{name}}} ({{count}} crate{{#unless (eq count 1)}}s{{/unless}})
{{/each}}

{{#each licenses}}
===============================================================================
{{{name}}}
===============================================================================

Applies to:
{{#each used_by}}
  - {{crate.name}} {{crate.version}}
{{/each}}

{{{text}}}

{{/each}}
